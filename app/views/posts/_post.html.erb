<%= tag.div class: %w[card my-2], data: { post_id: post.id } do %>
  <%= tag.div post.content, class: %w[card-body] %>
  <%= tag.div class: %w[card-footer text-body-secondary] do %>
    <%= tag.small post.user.name %>
    <%= tag.small h(post.created_at) %>
  <% end %>
<% end %>